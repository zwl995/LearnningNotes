#include <cstdio>
#include <cstdlib>
#include <algorithm>
using namespace std;

int a, b, c, d;

void solve()
{
  
}

int main()
{
  freopen("in.txt","r",stdin);
  freopen("out.txt","w",stdout);

  while(scanf("%d%d%d%d",&a,&b,&c,&d) && (a != 0 && b != 0 && c!= 0 && d != 0))
  {
    solve();
  }

}
